(function($) {
  $('.carousel').flickity({
    // options
    // cellAlign: 'left',
    //contain: true,
    //imagesLoaded: true,
    groupCells: true,
    prevNextButtons: false,
    draggable: true
  });
})(jQuery);
